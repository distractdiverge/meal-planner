name: Tests
on:
  push:
    paths:
      - '**/*.ts'
      - '**/*.js'
      - '**/*.json'

jobs:
  test:
    name: Update
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '10.x'
      - run: npm ci
      - run: npm test
